language: php

php:
  - 7.1
  - 7.2
  - 7.3

before_script:
  - sudo cp binaries/checkmol /usr/local/bin
  - sudo cp binaries/matchmol /usr/local/bin
  - sudo cp binaries/mol2svg /usr/local/bin
  - travis_retry composer update
  - cp .env.example .env
  - php artisan key:generate
  - mkdir storage/app/public/molfiles
  - mkdir storage/app/public/svg

script:
  - vendor/bin/phpunit
